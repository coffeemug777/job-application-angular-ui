import { createAction } from '@ngrx/store';

export const logIn = createAction('[User] logging in');
export const logOut = createAction('[User] logging out');
export const register = createAction('[User] register');
